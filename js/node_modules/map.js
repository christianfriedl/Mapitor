var m_node = require('node/node.js');
var m_area = require('node/area.js');

function Map() {
    /* @DEVELOPMENT { */
         var LEN = 0; if ( arguments.length !== LEN ) { throw new Error('arguments length is ' + arguments.length + ', should be ' + LEN); }
    /* @DEVELOPMENT } */
    this._width = 256;
    this._height = 256;
    this._area = m_area.area(this, null, 0, 0, this._width, this.height);
    console.log('map->area', this._area);
}

function map() {
    /* @DEVELOPMENT { */
         var LEN = 0; if ( arguments.length !== LEN ) { throw new Error('arguments length is ' + arguments.length + ', should be ' + LEN); }
    /* @DEVELOPMENT } */
    return new Map();
}


Map.prototype.constructor = Map;

/* m_area.Area */ Map.prototype.area = function() {
    console.log('map returns area', this._area);
    return this._area;
}

exports.Map = Map;
exports.map = map;
