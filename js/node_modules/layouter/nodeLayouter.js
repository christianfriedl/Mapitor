"use strict";

var m_node = require('node/node.js');

function NodeLayouter(/* m_node.Node */ p_node) {
    if ( arguments.length > 0 ) {
        /* @DEVELOPMENT { */
            var LEN = 1; if ( arguments.length !== LEN ) { throw new Error('arguments length is ' + arguments.length + ', should be ' + LEN); }
            var PARAM = p_node; if ( !(PARAM instanceof m_node.Node) ) { throw new Error('type error: param p_node should be of type m_node.Node'); }
        /* @DEVELOPMENT } */

        this._node = p_node;
    }
}

function nodeLayouter(/* m_node.Node */ p_node) {
    if ( arguments.length > 0 ) {
        /* @DEVELOPMENT { */
            var LEN = 1; if ( arguments.length !== LEN ) { throw new Error('arguments length is ' + arguments.length + ', should be ' + LEN); }
            var PARAM = p_node; if ( !(PARAM instanceof m_node.Node) ) { throw new Error('type error: param p_node should be of type m_node.Node'); }
        /* @DEVELOPMENT } */

        return new NodeLayouter(p_node);
    } else {
        return new NodeLayouter();
    }
}

NodeLayouter.prototype.constructor = NodeLayouter;

NodeLayouter.prototype.node = function(/* m_node.Node */ p_node) {
    if ( typeof(p_node) !== 'undefined' ) {
        this._node = p_node;
        return this;
    }
    return this._node;
};

exports.NodeLayouter = NodeLayouter;
exports.nodeLayouter = nodeLayouter;
