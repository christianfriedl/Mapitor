var PainterFactory = {
    forObject: function(/* object */ p_object) {
        /* @DEVELOPMENT { */
            var LEN = 1; if ( arguments.length !== X ) { throw new Error('arguments length is ' + arguments.length + ', should be ' + X); }
        /* @DEVELOPMENT } */
    }
};
module.exports = PainterFactory;
